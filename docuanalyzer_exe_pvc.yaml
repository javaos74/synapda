# azure-disk-pvc.yaml - 이미 만들었다면 생략
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synap-da-exe-pvc
  namespace : synap
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 100Mi # 다운로드할 파일 크기보다 크게 설정
